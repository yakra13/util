
#include <stdio.h>

int main(int argc, char** argv)
{
    FILE* f = fopen(argv[0]);
    if(!f)
    {

    }

    // find a "
    // ftell its position
    // extract to the next "
    // encrypt with argv[1]
    // replace in file
    // write the file
}